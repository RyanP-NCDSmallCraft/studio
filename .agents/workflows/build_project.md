---
description: instructions to compile and build the NextJS project
---
This workflow guides you through building the RegoCraft NextJS project.

// turbo
1. Run `npm install` to install dependencies.

// turbo
2. Run `npm run build` to compile the project.
